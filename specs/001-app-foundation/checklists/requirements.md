# Specification Quality Checklist: Фундамент приложения (Фаза 0)

**Purpose**: Validate specification completeness and quality before proceeding to planning
**Created**: 2026-08-14
**Feature**: [spec.md](../spec.md)

## Content Quality

- [x] No implementation details (languages, frameworks, APIs)
- [x] Focused on user value and business needs
- [x] Written for non-technical stakeholders
- [x] All mandatory sections completed

## Requirement Completeness

- [x] No [NEEDS CLARIFICATION] markers remain
- [x] Requirements are testable and unambiguous
- [x] Success criteria are measurable
- [x] Success criteria are technology-agnostic (no implementation details)
- [x] All acceptance scenarios are defined
- [x] Edge cases are identified
- [x] Scope is clearly bounded
- [x] Dependencies and assumptions identified

## Feature Readiness

- [x] All functional requirements have clear acceptance criteria
- [x] User scenarios cover primary flows
- [x] Feature meets measurable outcomes defined in Success Criteria
- [x] No implementation details leak into specification

## Notes

- Items marked incomplete require spec updates before `/speckit-clarify` or `/speckit-plan`

### Validation log

**Iteration 1** — обнаружены и исправлены:
- Требования и критерии успеха содержали названия конкретных технологий (Drift, go_router,
  Cubit, `AppClock`, `moodScore`, `installId`). Переписаны на языке поведения: «локальное
  хранилище», «единый механизм переходов», «единственный источник текущего момента», «оценка
  настроения», «идентификатор установки». Технологический выбор зафиксирован в конституции и
  относится к `/speckit-plan`, не к спецификации.
- Критерии успеха «работает быстро» / «не падает» заменены на измеримые: SC-001 (2 с холодный
  старт), SC-002 (100 мс переключение), SC-003 (10 перезапусков подряд), SC-004 (получасовой
  прогон), SC-009 (покрытие ≥70%).
- Граница фазы была размытой — добавлены разделы `Dependencies` и `Out of Scope`.

**Iteration 2** — все пункты чек-листа проходят.

**Iteration 3** (после `/speckit-clarify`, 4 вопроса) — все пункты по-прежнему проходят. Затронуты
разделы: Clarifications (новый), User Story 2, User Story 3, User Story 4, Edge Cases, Functional
Requirements, Key Entities, Success Criteria, Assumptions, Out of Scope.

Что изменилось по существу:
- День записи стал вычисляемой величиной (хранится точный момент в UTC), граница суток —
  настраиваемый «час начала дня». Сняло размытость прежней формулировки «календарный день».
- Появилось явное правило разрешения совпадений, когда две записи попадают в один вычисленный
  день (FR-009b, FR-009c, SC-012) — прямое следствие предыдущего решения.
- Задано поведение при недоступном хранилище (FR-021a…FR-021e, SC-013) — закрыт единственный
  сценарий, гарантированно приводивший к крашу на старте.
- Диагностика зафиксирована как строго локальная (FR-016a, FR-016b) — утверждение «данные не
  покидают устройство» больше не имеет исключений.

**Iteration 4** (после второго `/speckit-clarify`, 5 вопросов) — все пункты по-прежнему проходят.
Затронуты разделы: Clarifications, User Story 1, User Story 3, Functional Requirements, Key
Entities, Success Criteria.

Что изменилось по существу:
- Граница «сохранения раздела» проведена явно (FR-004a): живой процесс — да, после выгрузки —
  «Стол». Прежнее замечание об этом снято.
- «Час начала дня» получил тип и диапазон (FR-025a: целые 0–23) — снята неоднозначность формата
  до написания схемы хранилища.
- Тон реплики стал закрытым перечнем с нейтральным запасным значением (FR-010a, FR-010b, SC-015).
- Локальная диагностика ограничена консолью отладочной сборки (FR-016c) — на устройстве
  пользователя не появляется второго хранилища.
- Непроверяемое «без лавины сообщений» заменено правилом подавления дублей (FR-021f, SC-014).

**Iteration 5** (после `/speckit-checklist`, чек-лист `data-resilience.md`) — все пункты
по-прежнему проходят. Затронут раздел: Functional Requirements.

Устранены противоречия внутри спеки:
- FR-010b требовал записи в диагностику, FR-016c запрещал записывать что-либо в релизе —
  требование разведено по типам сборки, FR-016c дополнен общим правилом для таких формулировок.
- FR-017 (сброс при смене структуры) и FR-021a (запрет молчаливого удаления) описывали разные
  ситуации одинаковыми словами — граница проведена в FR-017a.
- Добавлен FR-016d: без отключения системного автобэкапа утверждение FR-016 «данные только на
  устройстве» имело скрытое исключение.
- FR-019a разделил виды ошибок хранилища (недоступно при старте / режим без сохранения / нет
  места / запись не найдена) — раньше все они сводились к одному «ошибка хранилища».
- Термин «режим только для просмотра» нормализован в «режим без сохранения» (FR-021d).

**Iteration 6** (закрытие чек-листа `data-resilience.md`, 34/34) — все пункты чек-листа качества
по-прежнему проходят. Затронуты разделы: User Story 3, Functional Requirements, Success Criteria,
Assumptions.

Закрыты оставшиеся пробелы: лаг границы суток (60 с) и поведение на DST; момент обнаружения смены
пояса; правило выбора при равных сохранённых моментах и при одновременных сохранениях; длина
текста о дне; состав перечня тонов и стартового состава персонажей; поведение `updatedAt`;
энтропия идентификатора установки и его судьба в режиме без сохранения; окно подавления дублей
(3 с) и его глобальность; ошибки запуска вне хранилища; доступность экранов до оболочки; способы
воспроизведения сбоев для SC-004/SC-013 и новый SC-016; допущение об объёме данных.

### Замечания к следующему этапу (не блокирующие)

- **Стоимость выбранной модели времени**: вычисление дня при чтении означает, что уникальность
  дня нельзя переложить на хранилище — она проверяется в коде при сохранении. На этапе
  `/speckit-plan` это надо учесть при выборе индексов и при написании проверок US4.
- **Экран «хранилище недоступно»** формально выходит за рамки «разделы — только заглушки», но
  необходим для FR-021b; в план его следует внести как отдельный экран.
